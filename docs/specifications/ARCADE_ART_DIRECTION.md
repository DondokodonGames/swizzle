# ARCADE ART DIRECTION — 「100円を投入したくなる」ゲーム内視覚言語

対象は**ゲーム内画面**(コードゲームのCanvas描画)。サービスUI(BridgeScreen等)のアートディレクションは
`docs/art-direction/`(GALLERY案)が正で、本書とは独立。

ゲームセンターの筐体は、電源が入っているだけで客の足を止め、100円を入れさせる。
その視覚言語を70年代から現代まで分解し、Swizzleの全ゲームに実装可能な規定として定義する。

---

## 1. アトラクトモードは3秒で遊びを売る(MUST)

ゲーセンの筐体はデモ画面で「何が起きるゲームか」を見せ続ける。ATTRACTの構成要素:

1. **ピクセルロゴ**: タイトルは `draw.text` の単発ではなく、縁取り+影+ハイライトの重ね打ち、
   または `draw.sprite` のビットマップフォントで「ロゴ」にする(bird_jumpのKOBETAN JUMP式)
2. **ゴースト実演**(v3で必須化): 手カーソル(`game.draw.hand`、WP62で追加)がAIプレイヤーとして
   実ゲームロジックを1サイクル遊んで見せる3秒ループ。成功例1回+(危険があれば)失敗例1回。
   静止一枚絵・説明文だけのATTRACTは**禁止**(PLAY_GRAMMAR_V3 §2.1)
3. **HI-SCORE 表示**: `game.best` の実値。ハードコードの偽ハイスコアは禁止(§QUALITY_STANDARD)
4. **点滅する投入誘導**: 「► 100円 投入 ◄ TAP TO START」を0.5秒周期で点滅
5. 世界観は**絵で**語る(キャラ・背景・小物)。説明テキストの掲示は禁止(PLAY_GRAMMAR_V3 §3)

## 2. 時代別スタイルパック(ゲームごとに1つ選択・MUST)

「全ゲームが同じネオン暗黒」からの脱却。**見た目の多様性はここが担保する**ので、
実在した各時代のハード表現を広く用意してある。

> **順序**: 様式は**最後に当てる分類**（[PRODUCTION_ORDER.md](./PRODUCTION_ORDER.md)）。
> 遊びの型 → 世界観 → 様式 の順で決める。**世界観に合わせて様式を選ぶのではなく**、
> 在庫のバランス（同じ様式に偏らせない）と拠点・客層への効き方で選ぶ。
> 様式を変えても別ゲームにはならない — 様式は差別化手段ではない。

> **命名**: ハードウェアの商標名は使わない（IP_SAFETY_RULES）。ドット様式そのものは権利ではないが、
> 「ファミコン」「スーファミ」等の名称は商標なので、時代と技法で言い換えている。

### 描けるものの制約（レシピの前提）

サンドボックスの描画は **`rect` / `circle` / `line` / `sprite` / `gradient` / `text`** だけ。
**任意多角形の塗りも、図形の回転も無い**（`iframeTemplate.ts`）。3D系の様式はこの制約下で再現する:

| やりたいこと | 手段 |
|---|---|
| 面（三角形・四角形）を塗る | **横1pxの矩形ストリップ**で走査線塗り（`for (y…) rect(xl, y, xr-xl, 1, col)`） |
| 図形を回す | 頂点座標を自前で回転計算し、上のストリップ塗りで描く |
| 奥行き | `sprite` の px スケールを距離で変える + 接地影 |
| ぼかし・発光 | 半透明の円/矩形を数枚重ねる |
| 中間色 | 市松ディザ（1px矩形の交互配置） |

機械可読な定義は `src/ai/code/stylePacks.ts`（単一の真実の源）。
各パックには **次元**（`2d` / `pseudo3d`）も持たせてあり、時代とは別軸で偏りを見られる。
ファイル冒頭に**宣言が必須**（v3バリデーターが `NO_STYLE_PACK` / `UNKNOWN_STYLE_PACK` で検査）:

```js
// スタイル: 80s NEON
var STYLE = { bg: [...], main: [...], accent: [...] };
```

### アーケード黎明〜80年代

| ラベル | 由来 | 色 | 作り方 |
|---|---|---|---|
| `70s VECTOR` | ベクタースキャン | 発光1〜3色 | **塗りを使わず線だけ**。`draw.line` を太さ違いで2〜3回重ねて発光を作る。黒地。オブジェクトは多角形の輪郭 |
| `70s MONO` | ラスター白黒 | 白 + 帯の単色 | 白ドット + 画面帯ごとの単色オーバーレイ(ブラウン管のセロハン再現)。矩形波の単音、無音の間が緊張 |
| `80s NEON` | 80sアーケード | 発光4色 | 濃紺〜黒グラデ + シアン/マゼンタ/イエロー/白。外周に薄い同色を重ねた疑似グロー。点滅が命 |
| `80s ISO` | クォータービュー | 6〜8色 | 菱形グリッドの床。高さは**影の距離**で示す。奥ほど小さくしない(等角) |

### 家庭用・携帯 8bit

| ラベル | 由来 | 色 | 作り方 |
|---|---|---|---|
| `8bit HOME` | 家庭用8bit機 | 3〜4色 + 黒 | 8×8/16×16の粗いドット、輪郭線なし。背景はタイル反復(市松・レンガ・星)。スクロールは1方向。音は矩形波2+三角ベース+ノイズ |
| `8bit HANDHELD` | 携帯モノクロ | 4階調(黄緑寄り) | `#0f380f`〜`#9bbc0f` の4階調のみ。**残像**(前フレームを薄く残す)と低コントラスト。画面枠を描く |
| `8bit PC MONITOR` | ホビーPC | 8色ベタ | 高解像度・低色数。細い線、テキスト枠のUI、市松ディザで中間色を作る |

### 16bit

| ラベル | 由来 | 色 | 作り方 |
|---|---|---|---|
| `90s 16bit` | 家庭用16bit | 多色・高彩度 | 背景2〜3層(空グラデ+遠景シルエット+近景)で奥行き。大きめキャラ、暗色輪郭+ハイライト。**もっとも汎用** |
| `90s BIG SPRITE` | 対戦/ベルト筐体 | 多色 | 画面の1/3を占める**巨大キャラ**。床に楕円影。間合いと構えで見せる。背景は横1層で十分 |
| `90s HANDHELD COLOR` | 携帯カラー機 | 低彩度・少色 | 小画面前提で**太く単純な形**。密度を上げない。彩度を落として日焼けした液晶の色味に |

### 疑似3D・プリレンダ

| ラベル | 由来 | 色 | 作り方 |
|---|---|---|---|
| `90s PRE-RENDER` | プリレンダCG | 暗め・金属質 | 粒状ノイズ(薄い矩形を散らす)、擬似奥行き、金属のハイライト。実写寄りの陰影 |
| `90s LOW POLY` | 初期3D家庭用機 | 面ベタ塗り | 輪郭は `line`、面は**横1pxストリップ**で塗る。頂点を毎フレーム微小に揺らす(ジッター)。遠景はフォグ色の矩形で潰す。テクスチャは貼らない |

### 2000年代〜現代

| ラベル | 由来 | 色 | 作り方 |
|---|---|---|---|
| `2000s ARCADE POP` | 音ゲー/メダル | 原色 + 白縁 | 明るい背景(白〜パステルグラデ)、太い白縁取り文字、光の柱・虹・星。「光り物」の物量 |
| `2000s HANDHELD PASTEL` | 携帯機 | パステル | 白縁の丸い形。画面を上下に分けて情報と遊びを置く(2画面の名残) |
| `2010s FLAT MOBILE` | スマホUI | ベタ塗り数色 | 影・グラデを使わない。丸角、大きい余白、アイコン的な形。判定はUIの色変化で示す |
| `NEO-RETRO` | 現代インディ | 限定4〜6色 | パレットを先に決め、その中だけで完結。大きいドット(12〜16px)。1色だけ強い差し色 |
| `MODERN AD-GAME` | 動画広告ゲーム | 高彩度・高コントラスト | 太い縁取り、大きいアイコン、飛ぶ数字(+100 / COMBO x3)。3秒で何をするか伝わる画面 |
| `PIXEL HD` | 現代の高精細ドット | 多色 + 光 | パララックス3層以上、光源を1つ決めて陰影を統一、4〜8フレームの細かいアニメ |
| `1BIT INK` | 2値表示 / 墨 | 白黒2値 | 中間色は**ディザ**(市松・網点)で作る。線の太さで距離と力を語る |

### 疑似3Dの系譜（2Dプリミティブで3Dの見え方を作る）

| ラベル | 由来 | 色 | 作り方 |
|---|---|---|---|
| `MODE7 PSEUDO` | 16bit の床回転拡大 | 少色 + 地平グラデ | 画面を横1pxストリップに分け、**奥ほど横方向を圧縮**して床の市松を描く。地平線から上は空グラデ。速度感はストリップのスクロール量で出す |
| `90s PRE-RENDER` | プリレンダCG | 暗め・金属質 | 背景は動かない1枚絵として描き込む。粒状ノイズ(薄い1px矩形を散らす)、金属のハイライト、キャラだけドットで動かす |
| `2000s BILLBOARD 3D` | 2000s 家庭用3D | 多色 + 影 | オブジェクトは板（`sprite`）のまま、**距離で px スケールを変える**。真下に楕円の接地影を必ず置く。奥のものほど彩度を落とす |
| `HD POST 3D` | HD期の3D | 低彩度・褐色寄り | 全体を褐色〜灰に寄せ、**ブルーム**(半透明円を2〜3枚重ね)と**ビネット**(画面四辺に黒の半透明矩形)。コントラストを意図的に潰す |
| `TOON SHADE` | セルシェード3D | 2段階陰影 + 黒線 | 先に一回り大きい黒で輪郭を描き、内側を**明・暗の2色だけ**で塗る。中間調を作らない |
| `VOXEL BLOCK` | 立方体ブロック | 面ごとに3明度 | 立方体を上面/左面/右面の3明度で描き、等角に積む。影は落とさず明度差だけで立体を出す |

### スマホの系譜

| ラベル | 由来 | 色 | 作り方 |
|---|---|---|---|
| `SKEUOMORPH` | 初期スマホUI | 質感テクスチャ | 木目・フェルト・革の質感を `gradient` + 細線で作る。ボタンは上明・下暗のグラデ + 白ハイライトで厚みを出す |
| `2010s FLAT MOBILE` | スマホUI(フラット期) | ベタ塗り数色 | 影・グラデを使わない。丸角、大きい余白、アイコン的な形 |
| `HYPERCASUAL 3D` | 現代ハイパーカジュアル | 白背景 + 単色 | 白〜淡色の背景に**単色の丸い塊**。真下に柔らかい影(半透明の楕円)。当たり判定が見た目どおりであることを最優先 |
| `MODERN AD-GAME` | 動画広告ゲーム | 高彩度・高コントラスト | 太い縁取り、大きいアイコン、飛ぶ数字(+100 / COMBO x3) |

> `2010s FLAT MOBILE` と `MODERN AD-GAME` は上の「2000年代〜現代」表にも載っている同じパック（系譜として再掲）。

**共通規定**: 1ゲーム = 基調3色 + アクセント2色を STYLE 宣言(スコアラー/レビューが検査可能)。
背景は `draw.gradient` + 遠景要素(黒一色はテーマ上の必然がある場合のみ)。
スキャンライン(8px)+ドット絵の「キャビネット感」は全スタイル共通で維持。

## 3. コイン投入動機の演出規定(MUST)

100円を入れさせるのは「見た目」ではなく**感情の設計**。全ゲームに以下を実装する:

| 感情 | 演出 | 実装 |
|------|------|------|
| **ジャックポット感** | スコアが爆発する瞬間を作る(コンボ倍率、ボーナスタイム、フィーバー) | `fx.popup('x8 COMBO!')` + 音程が上がっていくSE |
| **CELEBRATION** | 成功時は音と光を**飽和**させる。控えめな成功画面は禁止 | se_success + burst連発 + 点滅 + 画面全体の祝福 |
| **悔しさの言語化(ニアミス)** | 惜しい失敗ほど「あと1個!」「98%」「残り0.3秒だった」を明示 | RESULT画面に必達。失敗を次の100円に変換する装置 |
| **記録の祭り** | HI-SCORE更新は普通のクリアより派手に | NEW RECORD 点滅 + se_milestone + BridgeScreenのバッジと二段構え |
| **緊張の可聴化** | 残り時間わずか/ラストチャンスでBGMテンポ/音程が変わる | melodyのtempo切替 or 警告音ループ |

## 3.5 記号文法 — テキストなしでルールを伝える視覚言語(MUST)

指導文が禁止(PLAY_GRAMMAR_V3)のため、以下の記号を全ゲーム共通の「文法」として統一する。
プレイヤーは1本目で学んだ記号を800本すべてで再利用できる:

| 意味 | 記号 |
|------|------|
| 狙う物・押す物 | **明滅+白縁**、待機バウンス(押せる物は揺れる) |
| 危険物 | **赤系+トゲ形状**。出現の0.5〜0.8秒前に必ず予告(点滅マーカー/影/警告線) |
| ゴール | 旗・ゲート・光る出口(常時見える) |
| 残り時間 | 収縮するリング/バー(数字のみは不可) |
| 進捗 | 残りN個のアイコン列(消し込み)、伸びるゲージ、高度メーター |
| 操作ゾーン | 押せる領域は明度を上げ、縁を光らせる |
| 手本・誘導 | ゴーストハンド(`game.draw.hand`)+タップリング |

テーマ(台帳の theme_jp)はこの記号の**上に着せる皮**であり、記号の意味を壊してはならない
(例: お菓子テーマでも危険物はトゲ+赤系を保つ)。

## 4. キャラクターの最低要件

- 主役(操作対象 or 目標物)は**顔がある/名前が付けられる**モチーフにする(目2ドットで十分)
- 2〜4フレームで「生きている」動き(待機で揺れる、移動でバタバタ、成功でジャンプ)
- 失敗時にキャラがリアクションする(倒れる/驚く/焦げる) — 記号ではなく演者にする

## 5. 監査

- `npm run games:smoke` のスクリーンショット・コンタクトシートで人間が確認:
  「スタイルパックが判別できるか」「ATTRACTだけで遊びが分かるか」「キャラに顔があるか」
  「指導文が写っていないか」「危険物が記号文法(§3.5)通りか」
- `CodeQualityScorer` の audioVisual / goalEndings 軸が sprite / gradient / best / popup を静的検査
  (WP63 でテキストレス検査と attract_motion 実行時検査が加わる)
