# `src/marketing/` — 未実装スタブ（保留棚）

このディレクトリは将来のマーケティング自動化（Discord / Twitter / Instagram /
TikTok への投稿、ゲーム画面キャプチャ等）の**保留棚**であり、現時点では
**未実装のスタブ**です。

## 現状

- 各 `*Automation.ts` / `MarketingOrchestrator.ts` / `GameCaptureService.ts` は
  外部 API と未統合で、`success: false` を返すかシミュレーションでフェイルします。
- `package.json` の `marketing:*` スクリプトは存在しますが、実投稿は行いません。

## 扱い

- **削除しない**（外部 API 連携の承認待ちのため、意図的に保留）。
- 「実装済み機能」として見積もり・宣伝に使わないこと。
- 実装に着手する際は、まず各プラットフォームの API 認証情報・利用規約の確認から。

> 由来: WP31 の TODO 棚卸し / WP32 アーキテクチャレビュー §9。WP33 で削除ではなく
> 「保留棚として明記」する判断（承認シート #9）に基づき、このファイルを追加。
